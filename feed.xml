<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yc2367.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yc2367.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-27T14:48:46+00:00</updated><id>https://yc2367.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Quantization for LLM: A HW/SW Co-Design Perspective</title><link href="https://yc2367.github.io/blog/2026/quant-codesign/" rel="alternate" type="text/html" title="Quantization for LLM: A HW/SW Co-Design Perspective"/><published>2026-04-22T00:00:00+00:00</published><updated>2026-04-22T00:00:00+00:00</updated><id>https://yc2367.github.io/blog/2026/quant-codesign</id><content type="html" xml:base="https://yc2367.github.io/blog/2026/quant-codesign/"><![CDATA[<p><strong>( NOTE: For mobile users, please view this blog in landscape mode for better readibility. )</strong></p> <p>In this post, I will discuss quantization, an essential technique for efficient LLM deployment on hardware. My goal is to give an overview of LLM quantization from an algorithm/hardware co-design perspective. I will start from number representation, quantization basics, to advanced quantization techniques, as well as their hardware implications<d-footnote>This blog is not meant to be a survey paper on LLM quantization. For a more comprehensive list of LLM quantization papers, you may want to check out <a href="https://github.com/Kai-Liu001/Awesome-Model-Quantization">this page</a>.</d-footnote>.</p> <h2 id="i-number-representation">I. Number Representation</h2> <p>In general, quantization aims to minimize the hardware memory usage to store numbers and the computational cost to process these numbers, while preserving model accuracy<d-footnote>Although in practice, a small amount of accuracy degradation may be acceptable.</d-footnote>. Hardware is designed to store and process binary bits, i.e., ‘0’ and ‘1’, which are interpreted according to a number representation (also called number format). This representation defines <strong>how a binary number maps to a real-valued number</strong>. Modern AI hardware typically supports two types of number representations: <strong>Integer (INT)</strong> and <strong>Floating-Point (FP)</strong>, each can have different precisions as illustrated below:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Number_Representation.png" width="80%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <h2 id="i-1-integer-int"><sub>I-1. Integer (INT)</sub></h2> <p>Consider an \(\mathrm{N}\)-bit integer \(\mathrm{D_{N-1}\,{D_{N-2}\,\dots\,D_{0}}}\). Three commonly used integer representations are defined as follows:<d-footnote>Interestingly, these three representations differ only in how the most significant bit, D<sub>N-1</sub>, is interpreted.</d-footnote></p> <ul> <li> <p><strong>Sign-Magnitude Representation</strong>, with decimal value:</p> \[(-1)^{\mathrm{D_{N-1}}} \,\cdot\, \left(\,\mathrm{D_{N-2}\,2^{N-2} + D_{N-3}\,2^{N-3} \,+\dots+ D_{0}\,2^{0}}\,\right)\] <p></p> </li> <li> <p><strong>Two’s Complement Representation</strong>, with decimal value:</p> \[\mathrm{-D_{N-1}\,2^{N-1} + D_{N-2}\,2^{N-2} + D_{N-3}\,2^{N-3} \,+\dots+ D_{0}\,2^{0}}\] </li> <li> <p><strong>Unsigned Representation</strong>, with decimal value:</p> \[\mathrm{D_{N-1}\,2^{N-1} + D_{N-2}\,2^{N-2} \,+\dots+ D_{0}\,2^{0}}\] </li> </ul> <h2 id="i-2-floating-point-fp"><sub>I-2. Floating-Point (FP)</sub></h2> <p>A floating-point representation is characterized by three components: <strong>Sign (S), Exponent (E), Mantissa (M)</strong>. Since the sign always has a single bit, a floating-point representation with \(\mathrm{x}\)-bit exponent and \(\mathrm{y}\)-bit mantissa is often called \(\mathrm{\mathbf{ExMy}}\), which has the following binary pattern and decimal value:</p> \[\mathrm{S \; \underbrace{E_{x-1}\dots E_{0}}_{\text{Exponent}} \; \underbrace{M_{y-1}\dots M_{0}}_{\text{Mantissa}}} \,=\, \begin{cases} \,(-1)^{\,\mathrm{S}} \,\cdot\, \mathrm{2^{E-B} \,\cdot\, 1.{M}} &amp; \text{if } E\neq0 \\ \,(-1)^{\,\mathrm{S}} \,\cdot\, \mathrm{2^{1-B} \;\cdot\, 0.{M}} &amp; \text{if } E=0 \end{cases}\] <p>where \(\mathrm{E}\) and \(\mathrm{M}\) are interpreted as unsigned integers, the constant \(\mathrm{B = {2^{\,x-1}-1}}\) is a fixed “bias” that allows the actual exponent to be both positive and negative. Note that there is a leading bit before \(\mathrm{M}\), which makes the actual mantissa equal to \(1.{\mathrm{M}}\) or \(0.\mathrm{M}\). This leading bit is often called the hidden bit as it does not need to be stored. Instead, it can be determined during runtime by checking whether the value is normal \((\mathrm{E} \neq 0)\) or subnormal \((\mathrm{E} = 0)\).</p> <p>For LLM training and inference, the default number representations are the <a href="https://en.wikipedia.org/wiki/Single-precision_floating-point_format">32-bit IEEE floating-point format</a> (<strong>FP32-E8M23</strong>) and the <a href="https://en.wikipedia.org/wiki/Bfloat16_floating-point_format">16-bit brain floating-point format</a> (<strong>BF16-E8M7</strong>). In practice, BF16-E8M7 is often used to store model weights and activations<d-footnote>For recent LLMs, BF16-E8M7 is often preferred over the 16-bit IEEE floating-point format (FP16-E5M10), because it has a wider dynamic range that helps prevent overflow during training.</d-footnote>, whereas FP32-E8M23 is used to store intermediate results of numerically sensitive operations (e.g., partial sums in dot product and softmax).</p> <h2 id="i-3-hardware-for-int-and-fp-arithmetic"><sub>I-3. Hardware for INT and FP Arithmetic</sub></h2> <p>Given the importance of hardware efficiency for LLMs, it is useful to understand <strong>how a Multiply-Accumulate (MAC), the key operation in LLM training and inference, is carried out on hardware.</strong> Because a MAC consists of multiplication and addition, I will discuss the hardware implication of these two primitive operations under integer and floating-point representation.</p> <h3 id="integer-addition-and-multiplication"><sub>Integer Addition and Multiplication</sub></h3> <p>The simplest integer adder is <a href="https://en.wikipedia.org/wiki/Adder_(electronics)#Ripple-carry_adder">Ripple-Carry Adder</a>, whose area and delay complexities ore \(\mathrm{O(N)}\)<d-footnote>This bound assumes that a binary full-adder has a unit area and delay complexity of O(1).</d-footnote> for \(\mathrm{N}\)-bit integer addition. Other adder architectures include <a href="https://en.wikipedia.org/wiki/Carry-select_adder">Carry-Select Adder</a>, <a href="https://en.wikipedia.org/wiki/Carry-lookahead_adder">Carry-Lookahead Adder</a>, etc., which offer different trade-offs between area and delay.</p> <p>The simplest <a href="https://en.wikipedia.org/wiki/Binary_multiplier">integer multiplier</a> multiplies every bits of two multiplicands via logical AND operations, followed by reducing these binary products via adder chains. This leads to an area complexity of \(\mathrm{O(N^2)}\) for \(\mathrm{N}\)-bit integer multiplication. Modern integer multipliers employ more efficient architectures, such as <a href="https://en.wikipedia.org/wiki/Wallace_tree">Wallace Tree</a> and <a href="https://en.wikipedia.org/wiki/Dadda_multiplier">Dadda Tree</a>, for fast reduction of binary products.</p> <h3 id="floating-point-multiplication"><sub>Floating-Point Multiplication</sub></h3> <p>The floating-point MAC is rather complicated as it needs to deal with exponent. To simply the discussion, I will only focus on <em>normal</em> floating-point numbers, whose binary exponent is larger than zero.</p> <p>Consider two normal floating-point numbers \(\mathrm{F}_{1}\) and \(\mathrm{F}_{2}\,\):</p> \[\begin{aligned} \mathrm{F_1} \,&amp;=\, (-1)^{\,\mathrm{S}_{1}} \,\cdot\, 2^{\mathrm{E}_{1}-\mathrm{B}} \,\cdot\, 1.\mathrm{M}_{1} \\ \mathrm{F_2} \,&amp;=\, (-1)^{\,\mathrm{S}_{2}} \,\cdot\, 2^{\mathrm{E}_{2}-\mathrm{B}} \,\cdot\, 1.\mathrm{M}_{2} \end{aligned}\] <p>The multiplication product is:</p> \[\mathrm{P} = \mathrm{F_1} \cdot \mathrm{F_2} \,=\, (-1)^{\mathrm{S}_{1\,} \oplus\, \mathrm{S}_{2}} \,\cdot\, 2^{\mathrm{E}_{1} + \mathrm{E}_{2}-\mathrm{2B}} \,\cdot\, \underbrace{(\,1.\mathrm{M}_{1} \times 1.\mathrm{M}_{2}\,)}_{\text{Un-norm Mantissa}}\] <p>Based on the definition of floating-point representation as described previously, the sign, exponent, and mantissa of a product should be:</p> \[\begin{aligned} \mathrm{S_{P}} \,&amp;=\, \mathrm{S}_{1} \oplus\, \mathrm{S}_{2} \\[0.3em] \mathrm{M_{P}} \,&amp;=\, \begin{cases} \; (\,1.\mathrm{M}_{1} \times 1.\mathrm{M}_{2}\,) &amp; \text{if un-norm mantissa} \leq 2 \\[0.1em] \;(\,1.\mathrm{M}_{1} \times 1.\mathrm{M}_{2}\,) \;/\; 2 &amp; \text{if un-norm mantissa} &gt; 2 \end{cases} \\[0.3em] \mathrm{E_{P}} \,&amp;=\, \begin{cases} \;\mathrm{E}_{1} + \mathrm{E}_{2}-\mathrm{B} &amp; \quad \text{if un-norm mantissa} \leq 2 \\[0.1em] \;\mathrm{E}_{1} + \mathrm{E}_{2}-\mathrm{B} + 1 &amp; \quad \text{if un-norm mantissa} &gt; 2 \\ \end{cases} \\ \end{aligned}\] <p>The above formula shows that:</p> <ul> <li>The product sign bit is simply a logical XOR between the sign bits of \(\mathrm{F}_{1}\) and \(\mathrm{F}_{2}\,\).</li> <li>The product mantissa can be obtained via unsigned integer multiplication between two input mantissa (\(1.\mathrm{M}_{1}\) and \(1.\mathrm{M}_{2}\)). A conditional check (a.k.a normalization) is needed to determine whether the un-normalized mantissa product is larger than 2. If yes, the un-normalized mantissa should be divided by 2, i.e., normalized to the range \([1, 2)\).</li> <li>The product exponent can be obtained via unsigned integer addition / subtraction between two input exponents (\(\mathrm{E}_{1}\) and \(\mathrm{E}_{2}\)) and the exponent bias (\(\mathrm{B}\))<d-footnote>Note that we only subtract a single bias from (E<sub>1</sub> + E<sub>2</sub>), since the standard floating-point representation will automatically subtract another exponent bias.</d-footnote>. As discussed before, integer adder and multiplier have area complexity of \(\mathrm{O(N)}\) and \(\mathrm{O(N^2)}\), respectively. Since the cost of XORing the sign bit is negligible, the area complexity of a \(\mathrm{\mathbf{ExMy}}\) floating-point multiplier is roughly \(\mathrm{O(\,2x +(y+1)^2\,)}\). The following table shows the synthesized combinational logic area of BF16, FP16, and FP32 multipliers without the normalization stage<d-footnote>In a MAC unit, the normalization stage is placed after the accumulator.</d-footnote>, under 28nm technology. It can be observed that the area complexity offers a good estimation of the relative multiplier area across different floating-point representations.</li> </ul> <table style="width: 60%; border-spacing: 5px; margin-left: 1.5em; margin-top: -1.2em; margin-bottom: 1.5em;"><thead> <tr> <th colspan="1"> <br/>Representation </th> <th colspan="1"> <br/>Area Complexity </th> <th colspan="1"> <br/>Multiplier Area </th> </tr></thead> <tbody> <tr> <td> BF16-E8M7 </td> <td> 80 (1×) </td> <td> 167.2 um<sup>2</sup> (1×) </td> </tr> <tr> <td> FP16-E5M10 </td> <td> 131 (1.64×) </td> <td> 260.9 um<sup>2 </sup> (1.56×) </td> </tr> <tr> <td> FP32-E8M23 </td> <td> 592 (7.4×) </td> <td> 1166.8 um<sup>2</sup> (6.98×) </td> </tr> </tbody></table> <h3 id="floating-point-addition"><sub>Floating-Point Addition</sub></h3> <p>Again, consider two normal floating-point numbers \(\mathrm{F}_{1}\) and \(\mathrm{F}_{2}\,\):</p> \[\begin{aligned} \mathrm{F_1} \,&amp;=\, (-1)^{\,\mathrm{S}_{1}} \,\cdot\, 2^{\,\mathrm{E}_{1}-\mathrm{B}} \,\cdot\, 1.\mathrm{M}_{1} \\ \mathrm{F_2} \,&amp;=\, (-1)^{\,\mathrm{S}_{2}} \,\cdot\, 2^{\,\mathrm{E}_{2}-\mathrm{B}} \,\cdot\, 1.\mathrm{M}_{2} \end{aligned}\] <p>Let’s assume \(\mathrm{E}_{1} \geq \mathrm{E}_{2\,}\), then \(\mathrm{F}_{2}\) can be re-written as:</p> \[\mathrm{F_2} \,=\, (-1)^{\,\mathrm{S}_{2}} \,\cdot\, 2^{\,\mathrm{E}_{1}-\mathrm{B}} \,\cdot\, \left(\,1.\mathrm{M}_{2} \;/\; 2^{\,\mathrm{E}_{1}-\mathrm{E}_{2}}\,\right) \,=\, (-1)^{\,\mathrm{S}_{2}} \,\cdot\, 2^{\,\mathrm{E}_{1}-\mathrm{B}} \,\cdot\, \left(\,1.\mathrm{M}_{2} \gg (\mathrm{E}_{1}-\mathrm{E}_{2})\,\right)\] <p>The above step is called “Exponent Alignment”, where the two input exponents are compared and aligned to the larger one. Since we increase the exponent of \(\mathrm{F}_{2}\) by \((\,\mathrm{E}_{1}-\mathrm{E}_{2}\,)\), its mantissa needs to be divided by \(2^{\,\mathrm{E}_{1}-\mathrm{E}_{2}}\), which is equivalent to right-shifting the mantissa by \((\mathrm{E}_{1}-\mathrm{E}_{2})\) bits.</p> <p>The addition sum can then be expressed as:</p> \[\mathrm{F_1} + \mathrm{F_2} \,=\, 2^{\,\mathrm{E}_{1} - \mathrm{B}} \,\cdot\, \underbrace{\left[\; (-1)^{\,\mathrm{S}_{1}} \cdot 1.\mathrm{M}_{1} \;+\; (-1)^{\,\mathrm{S}_{2}} \cdot \left(\,1.\mathrm{M}_{2} \gg (\mathrm{E}_{1}-\mathrm{E}_{2})\,\right) \;\right]}_{\text{Un-norm Mantissa}}\] <p>The sum’s sign and mantissa are derived from the above un-normalizaed mantissa through a hardware normalization block, which does some post-processing to ensure the final mantissa is normalized to the range \([1, 2)\).</p> <p>Based on the above analysis, you may notice that it’s not straightforward to derive a good upper bound for the floating-point adder’s area complexity. There are also many low-level design considerations that vary across different hardware vendors when they design a floating-point adder. For example, how many bits should we reserve for accumulating the shifted mantissa \(1.\mathrm{M}_{2} \gg (\mathrm{E}_{1}-\mathrm{E}_{2})\) after exponent alignment? At FP32-E8M23, the largest and smallest representable exponents are \(127\) and \(-126\), respectively. This means that the 24-bit input mantissa (including the hidden bit) can be right-shifted by up to \(253\) bits, leading to a total precision of \(277\) bits for accumulating two aligned mantissa, which is clearly not a practical choice. Thus, in reality, the shifted mantissa is always truncated and accumulated with much lower precision<d-footnote>For a given hardware platform, the exact precision of its mantissa accmulator can often be determined through reverse engineering.</d-footnote>. This introduces a trade-off between numerical accuracy and hardware cost: allocating less precision to accumulate the shifted mantissa reduces hardware cost but harms numerical accuracy<d-footnote>For instance, <a href="https://arxiv.org/abs/2412.19437">DeepSeek-V3</a> finds that on NVIDIA H800 GPUs, the accumulation precision of FP8 MAC is only 14 bits, which can introduce a maximum relative error of nearly 2% compared to using FP32 accumulation precision.</d-footnote>.</p> <h2 id="ii-quantization-basics">II. Quantization Basics</h2> <p>Let’s now jump into quantization: one of the most popular methods to improve the hardware efficiency of LLMs.</p> <h2 id="ii-1-clssification-of-quantization-methods"><sub>II-1. Clssification of Quantization Methods</sub></h2> <p>Before diving into technical details, I would like to classify existing quantization methods into two broad categories: <strong>compute-based</strong> and <strong>memory-based</strong>, depending on how they interpret the low-precision operands in hardware<d-footnote>This is only one of many ways to classify quantization methods. For instance, one could also classify different methods based on their target quantized operands, e.g., weight-only quantization and weight-activation quantization, as described in <a href="https://arxiv.org/abs/2511.06838">this paper</a>.</d-footnote>. Below is a visualization:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Memory_vs_Compute_Quantization.png" width="60%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <ul> <li>Compute-based Quantization: The retrieved low-bit data from memory <strong>can</strong> be directly used to perform MAC. Examples include quantizing a tensor from BF16 to INT8 / FP8 / FP4, etc.</li> <li>Memory-based Quantization: The retrieved low-bit data from memory <strong>cannot</strong> be used to perform MAC. Instead, the low-bit data serves as an index to access another memory, which is often called a codebook and stores high-precision data (e.g., in BF16). The codebook access returns the actual data that is used to perform MAC.</li> </ul> <p>Compute-based quantization enables computation on low-bit MAC hardware, whereas memory-based quantization requires an additional memory access to the codebook cache before performing computation on BF16 MAC hardware. Hence, at the same model compression ratio, compute-based quantization typically consumes significantly less area and energy, making it the favorable approach in modern LLMs<d-footnote>Examples include DeepSeek-V3, DeepSeek-V4, Qwen3.5, etc.</d-footnote> and AI chips<d-footnote>Examples include NVIDIA GPU, Meta MTIA, Tesla AI4, Microsoft MAIA, etc.</d-footnote>. <strong>In the remaining of this blog, I will focus on discussing compute-based quantization.</strong></p> <h2 id="ii-2-mathematical-background"><sub>II-2. Mathematical Background</sub></h2> <p>Given a tensor represented in high-precision floating-point (e.g., BF16), the purpose of quantization is to convert this tensor to a lower-precision format (e.g., INT8 / FP8), where every tensor element is mapped to its closest quantization value. However, directly mapping a high-precision number to a low-precision format can introduce many issues, such as <strong>overflow</strong> and <strong>underflow</strong>, which are illustrated below:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Overflow_Underflow.png" width="90%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>The above example quantizes a tensor of four elements to the 3-bit sign-magnitude integer (INT3) representation, and measures the resulting root-mean-square (RMS) quantization error. As shown in the top-left figure, if the original tensor contains very small values that are close to zero, directly casting to INT3 will make all values underflow to zero, resulting in catastrophic information loss. Similarly, as shown in the bottom left-figure, if the original tensor contains very large values that overflow the INT3 range, directly casting to INT3 will clamp these large values to the endpoint of INT3, resulting in significant saturation error.</p> <p>To address the overflow and underflow issues, standard quantization linearly <strong>scales</strong> the original tensor to the quantized value range, then rounds each scaled tensor value to its nearest quantization value. After quantization, a re-scaling / dequantization step is performed at the end to map the quantized tensor back to the original tensor’s range. This procedure is summarized by the following equations:</p> \[\mathrm{S} = \frac{ |\mathrm{X}|_{\text{max}} }{ \mathrm{Q}_{\text{max}} } ; \ \ \ \mathrm{X_\mathrm{S}} = \frac{\mathrm{X}}{\mathrm{S}} ; \ \ \ \mathrm{X_Q} = \texttt{Round}\left(\,\mathrm{X_\mathrm{S}}\,,\, \mathrm{Q}\,\right) ; \ \ \ \mathrm{X_\mathrm{D}} = \mathrm{X_Q} \cdot \mathrm{S}\] <p>where \(\mathrm{X}\) is the original high-precision tensor, \(\mathrm{Q}\) is the set of quantization values defined by a low-bit number representation, \(\mathrm{S}\) is a high-precision scale factor, \(\mathrm{X_{S}}\) is the scaled tensor, \(\mathrm{X_{Q}}\) is the quantized tensor, and \(\mathrm{X_{D}}\) is the dequantized tensor. As shown in the above example, scaling can effectively mitigates the impact of overflow / underflow. When the tensor values are very small, scaling can enlarge the tensor range so that many elements are mapped to meaningful quantization values. When the tensor values are very large, scaling can prevent large saturation error.</p> <p>Because of scaling, if two sets of quantization values, \(\mathrm{Q_{1}}\) and \(\mathrm{Q_{2}}\), differ only by a constant factor, then they produce mathematically equivalent results after dequantization:</p> \[\begin{aligned} \text{Assume}: \quad &amp;\mathrm{Q_{2}} = c\,\mathrm{Q_{1}} \\[0.3em] \text{With }\mathrm{Q_{1}}: \quad &amp; \mathrm{S_{1}} = \frac{ |\mathrm{X}|_{\text{max}} }{ \mathrm{Q}_{1,\text{max}} } ; \ \ \mathrm{X_{S_{1}}} = \frac{\mathrm{X}}{\mathrm{S_{1}}} ; \ \ \mathrm{X_{Q_{1}}} = \texttt{Round}\left(\mathrm{X_{S_{1}}}, \mathrm{Q_{1}}\right) ; \ \ \mathrm{X_{D_{1}}} = \mathrm{X_{Q_{1}}} \cdot \mathrm{S_{1}} \\[0.3em] \text{With }\mathrm{Q_{2}}: \quad &amp; \mathrm{S_{2}} = \frac{ \mathrm{S_{1}} }{ c } ; \ \ \mathrm{X_{S_{2}}} = \frac{c\,\mathrm{X}}{\mathrm{S_{1}}} ; \ \ \mathrm{X_{Q_{2}}} = \texttt{Round}\left(c\,\mathrm{X_{S_{1}}}, \mathrm{c\,Q_{1}}\right) = c\,\mathrm{X_{Q_{1}}} ; \ \ \mathrm{X_{D_{2}}} = \mathrm{X_{Q_{2}}} \cdot \mathrm{S_{2}} = \mathrm{X_{D_{1}}} \\[0.3em] \end{aligned}\] <p>For example, at 4-bit quantization, you might have seen some papers using a format called E1M2, which has the set of quantization values \(\pm\{\,0,\, 0.25,\, 0.5,\, 0.75,\, 1,\, 1.25,\, 1.5,\, 1.75\,\}\). This is actually equivalent to INT4 quantization with the set of values \(\pm\{\,0,\, 1,\, 2,\, 3,\, 4,\, 5,\, 6,\, 7\,\}\).</p> <h2 id="ii-3-hardware-benefits-of-quantized-gemm"><sub>II-3. Hardware Benefits of Quantized GEMM</sub></h2> <p>By reducing the operand bit-width, quantization not only decreases the memory footprint, but also enables more efficient computation on low-precision MAC hardware. When the baseline LLM stores model weights and input activations in BF16, its linear layer will perform a GEMM using BFP16 multiply and FP32 accumulation. As an example, if we employ per-tensor quantization to convert model weights and input activations to INT8, the linear layer can instead perform a GEMM using INT8 multiply and INT32 accumulation, followed by dequantization that multiplies the output matrix with two scale factors. This is visualized below:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Quantized_GEMM.png" width="90%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>To understand the hardware implication of these two approaches, we can break down the hardware cost into two parts: memory and computation. The memory can be quantified as the total number of bytes required to store weights, inputs, and outputs. Accordingly, the memory costs of baseline and quantized GEMMs are:</p> \[\begin{aligned} \text{Baseline:} &amp; \ \ 2MK + 2KN + 4MN \\ \text{Quantized:} &amp; \ \ MK + KN + 4MN \end{aligned}\] <p>For computation, recall that a GEMM with size \(M\times K \times N\) requires \(MKN\) MAC operations. For dequantization, the scale factors \(\mathrm{S_{W}}\) and \(\mathrm{S_{A}}\) can be pre-multiplied and stored as a constant. Multiplying this constant with the output matrix requires \(MN\) multiplications. Thus, the computation costs of baseline and quantized GEMMs are:</p> \[\begin{aligned} \text{Baseline:} &amp; \ \ MKN \cdot \text{MAC}_{\text{BF16-FP32}} \\ \text{Quantized:} &amp; \ \ MKN \cdot \text{MAC}_{\text{INT8-INT32}} \,+\, (MN+1) \cdot \text{MUL}_{\text{FP32}} \end{aligned}\] <p>In modern LLMs, the \(K\)-dimension is usually very large (e.g., several thousands), making the cost of dequantization negligible compared to MAC. Consequently, the compute cost is roughly proportional to the cost of MAC units. The following table summarizes the synthesized area and energy of MAC units at different input-output precisions under 28nm technology. It can be observed that integer quantization can significantly reduce the computation cost compared to high-precision floating-point operations<d-footnote>In some cases, a quantization strategy chooses to maintain the original 16-bit precision to avoid unacceptable accuracy loss, and instead uses a more hardware-efficient number representation to reduce the computation cost. For instance, INT16 does not save memory cost over BF16, but it can reduce the area and energy of MAC by ~1.8×.</d-footnote>.</p> <table style="width: 70%; border-spacing: 5px; margin-left: 1.5em; margin-top: -1em; margin-bottom: 1.5em;"><thead> <tr> <th rowspan="2"> <br/>Multiplier </th> <th rowspan="2"> <br/>Accumulator </th> <th colspan="2"> <br/>Area (um<sup>2</sup>) </th> <th colspan="2"> <br/>Energy (pJ) </th> </tr> <tr> <th colspan="1"> <br/>Value </th> <th colspan="1"> <br/>Ratio </th> <th colspan="1"> <br/>Value </th> <th colspan="1"> <br/>Ratio </th> </tr></thead> <tbody> <tr> <td> INT8 </td><td> INT32 </td><td>213.8 </td><td>1× </td><td>0.16 </td><td>1× </td> </tr> <tr> <td> INT8 </td><td> INT48 </td><td>286.2 </td><td>1.34× </td><td>0.22 </td><td>1.38× </td> </tr> <tr> <td> INT16 </td><td> INT48 </td><td>516.2 </td><td>2.41× </td><td>0.36 </td><td>2.25× </td> </tr> <tr> <td> BF16 </td><td> FP32 </td><td>929.4 </td><td>4.35× </td><td>0.63 </td><td>3.94× </td> </tr> <tr> <td> FP32 </td><td> FP32 </td><td>1994.7 </td><td>9.33× </td><td>1.28 </td><td>8× </td> </tr> </tbody></table> <h2 id="iii-advanced-quantization-techniques">III. Advanced Quantization Techniques</h2> <p>In this section, I will discuss several techniques for improving the performance of quantized LLM inference. Many of these techniques are widely adopted in SoTA LLMs and AI chips. <strong>From an algorithm-hardware co-design perspective, a good quantization strategy introduces a trade-off between model accuracy, memory footprint, and computational efficiency.</strong> These trade-offs will also be examined throughout this section.</p> <h2 id="iii-1-block-wise-quantization"><sub>III-1. Block-Wise Quantization</sub></h2> <p>Block-wise quantization is employed in many recent LLMs such DeepSeek-V3, GPT-OSS, and Kimi-K2. Before diving into this technique, let’s first understand the concept of outliers. An outlier is defined as a value whose magnitude is significantly larger than the rest of values in a tensor. It is well known that LLM tensors contain extreme outliers, and preserving the numerical fidelity of these outliers is the key to reducing quantization error and improving model performance<d-footnote>For more details, interested readers can checkout <a href="https://arxiv.org/abs/2208.07339">this paper</a>.</d-footnote>.</p> <p>Quantization granularity measures how many elements are scaled and quantized together. For example, the coarsest quantization granularity is simply <strong>Tensor-Wise Quantization</strong>. But at this granularity, even a single outlier can significantly squeeze most of the remaining values, making them underflow to zero after quantization. This phenomenon is visualized in the following top-left figure, which uses INT4 sign-magnitude representation to quantize a \(3\times4\) tensor. To mitigate the impact of outliers, we can reduce the quantization granularity so that fewer values are quantized together. For example, <strong>Row-Wise Quantization</strong> applies independent scaling to each row of a tensor, limiting the effect of an outlier to values within the same row, as illustrated in the following top-right figure. On top of row-wise quantization, the granularity can be further reduced by partitioning each row into smaller segments / blocks, an approach called <strong>Block-Wise Quantization</strong>, which further constrains the impact of outliers to a small block. This is illustrated in the following bottom figure, where a row is partitioned into two blocks:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Quantization_Granularity.png" width="100%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p><strong>Why Does Block-Wise Quantization Help?</strong> Let’s analyze how quantization affects the maximum element \(\mathrm{X}_{\text{max}}\)<d-footnote>Here, the "maximum element" refers to the element with maximum magnitude, thus can be either positive or negative.</d-footnote>, without caring about granularity. In addition, let’s assume the quantized number representation is symmetric with respect to zero (i.e., \(\mathrm{Q}_{\text{max}} = -\mathrm{Q}_{\text{min}}\,\)), which holds for most number representations such as floating-point and sign-magnitude integer. According to the definition of quantization, we have:</p> \[\begin{aligned} \mathrm{S} &amp;= \frac{ |\mathrm{X}|_{\text{max}} }{ \mathrm{Q}_{\text{max}} } = \frac{ |\mathrm{X}_{\text{max}}| }{ \mathrm{Q}_{\text{max}} } \\[0.3em] \mathrm{X_{\text{max},\,S}} &amp;= \frac{ \mathrm{X}_{\text{max}}}{\mathrm{S}} = \frac{ \mathrm{X}_{\text{max}}\cdot \mathrm{Q}_{\text{max}} }{ |\mathrm{X}_{\text{max}}| } = \mathrm{X}_\text{sign} \cdot \mathrm{Q}_{\text{max}} \\[0.3em] \mathrm{X_{\text{max},\,Q}} &amp;= \texttt{Round-To-Nearest}\left(\,\mathrm{X_{\text{max},\,S}}\,,\, \mathrm{Q}\,\right) = \mathrm{X}_\text{sign} \cdot \mathrm{Q}_{\text{max}} \\[0.3em] \mathrm{X_{\text{max},\,D}} &amp;= \mathrm{X_{\text{max},\,Q}} \cdot \mathrm{S} = \left(\,\mathrm{X}_\text{sign} \cdot \mathrm{Q}_{\text{max}}\,\right) \,\cdot\, \frac{ |\mathrm{X}_{\text{max}}| }{ \mathrm{Q}_{\text{max}} } = \mathrm{X}_{\text{max}} \end{aligned}\] <p>The above last equation unveils a critical insight: <strong>If the scale factor is accurately computed (e.g., using high precision such as FP32), then the original maximum element remains unchanged after dequantization, regardless of the quantization granularity.</strong> In other words, the maximum element of a granularity has no quantization error. Therefore, under a finer quantization granularity, more maximum elements can be represented accurately, leading to lower total quantization error.</p> <p><strong>Algorithm-Hardware Trade-Off under Different Granularity:</strong> As discussed above, an obvious benefit of reduced quantization granularity is lower quantization error, which potentially offers better model accuracy. However, this algorithmic benefit comes with additional computation and memory costs. Below, I try to visualize the computation flow of performing a \(M\times K \times N\) GEMM under different INT4 quantization granularity:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Quantization_Granularity_Hardware.png" width="100%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <ul> <li><strong>Tensor-Wise Quantization</strong>: This is the coarsest granularity, where the GEMM requires \(MKN\) low-precision MAC operations. For dequantization, the FP32 scale factors \(\mathrm{S_{W}}\) and \(\mathrm{S_{A}}\) can be pre-multiplied and stored as a constant, and multiplying this constant with the output matrix requires \(MN\) FP32 multiplications.</li> <li><strong>Row-Wise Quantization</strong>: The whole GEMM still requires \(MKN\) low-precision MAC operations. But for dequantization, the row-wise scale factors of weight and activation are vectors of size of \(M \times 1\) and \(N \times 1\), respectively. These two vectors first perform an outer product, which is then element-wise multiplied with the output matrix, leading to \(2MN\) FP32 multiplications.</li> <li><strong>Block-Wise Quantization</strong>: Since a matrix row is partitioned into multiple blocks, the complete row-wise dot product will contain \(B\) partial sums, where \(B\) is the number of blocks. This means that the whole GEMM is partitioned into \(B\times\) small GEMMs, each of size \((M \times K/B \times N)\) with row-wise quantization (a block of the original large GEMM can be viewed as a row of the small GEMM). The total number of low-precision MAC operations remains \(B \times (M \times K/B \times N) = MKN\). The tricky part comes to the dequantization process: every small GEMM requires \(2MN\) multiplications as in row-wise quantization, leading to a total of \(2BMN\) FP32 multiplications. But the dequantized output matrix of every small GEMM only forms a partial sum, which also needs to be reduced over all blocks, leading to a total of \((B-1)MN\) extra FP32 additions.</li> </ul> <p>One thing I didn’t explain in the above figure, which you might find a little weird, is the output matrix precision. When I talk about BF16 GEMM and INT8 GEMM in previous sections, I used FP32 and INT32 as the output precision. However, the output matrix precision can be reduced according to the input precision, which in turn reduces the hardware accumulator precision and cost. Taking the above example, the sign-magnitude INT4 representation has a numerical range of \([-7, 7]\), and multiplying two INT4 numbers yields a range of \([-49, 49]\). Consider a trillion-parameter LLM such as <a href="https://huggingface.co/moonshotai/Kimi-K2.5/blob/main/config.json">Kimi-K2.5</a>, whose maximum dot product size of \(18432\). Under tensor-wise and row-wise INT4 quantization<d-footnote>Here, I am only using Kimi-K2.5 as an illustrative example to describe how we can optimize the hardware accumulator precision considering the quantization precision and dot product size. In practice, Kimi-K2.5 keeps input activations in BF16 and applies INT4 block-wise quantization to weights. More generally, tensor-wise or row-wise quantization is rarely used at very low precisions such as 4 bits.</d-footnote>, the INT4 dot product output has a range of \([-903168, 903168]\), which can be represented with a 21-bit sign-magnitude integer<d-footnote>In practice, the output accumulator precision may be further optimized depending on the data statistics, as it's impossible that all quantized INT4 values are -7 and 7.</d-footnote>. Similarly, for block-wise INT4 quantization under a typical block size of 128, the dot product output has a range of \([-6272, 6272]\), which can be represented with a 14-bit sign-magnitude integer.</p> <p>The memory costs of different quantization granularity are much simpler to analyze. In addition to weight, input, and output, quantization stores extra metadata such as the scale factors. For tensor-wise and row-wise quantization, the memory cost of scale factors is pretty negligible when the dot product size is large enough, which is the case for modern LLMs. However, this assumption may not hold for block-wise quantization. For example, with a block size of \(128\), an FP32 scale factor introduces an overhead of \(32/128 = 0.25\) bits per element. As the block size further decreases (to improve model performance), the storage overhead of scale factors can no longer be ignored. Thus, for the next quantization technique, I will discuss how to reduce the overhead of scale factors.</p> <h2 id="iii-2-scale-factor-quantization"><sub>III-2. Scale Factor Quantization</sub></h2> <p>While block-wise quantization brings algorithmic benefits by reducing quantization error, it incurs additional hardware cost from storing numerous FP32 scale factors and performing more FP32 multiplications / additions during dequantization. This overhead restricts the use of very samll block sizes, which are often critical for achieving high accuracy under 4-bit quantization.</p> <p>The question is: <strong>How can we reduce the cost of scale factors? The answer is surprisingly straightforward: if quantization can reduce the memory and computation costs of tensor elements, why not apply it to scale factors?</strong> Building on this idea, the industry has proposed two methods to quantize the block scale factor: <a href="https://www.opencompute.org/documents/ocp-microscaling-formats-mx-v1-0-spec-final-pdf">Microscaling (MX)</a> and <a href="https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/">NVIDIA’s (NV)</a> approach. Both quantize the scale factor to 8 bits, but using different number representations<d-footnote>Besides the scale factor representation, the official MX and NV quantization recipe also set fixed block sizes of 32 and 16, respectively. However, the concept of block size is not strictly tied to these two approaches. For instance, one can choose to reduce the MX block size from 32 to 16, as did in <a href="https://arxiv.org/abs/2603.08713">this paper from Meta</a>. Therefore, I will focus on discussing the scale factor representation without caring too much about the block size.</d-footnote>: MX uses the 8-bit power-of-two format (E8M0), whereas NV uses the 8-bit floating-point format with 4-bit exponent and 3-bit mantissa (FP8-E4M3). Below is a visualization of how MX and NV represent scale factors, assuming the block elements are quantized to FP4 (i.e., the popular MXFP4 and NVFP4 formats):</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/MXFP4_NVFP4_Overview.png" width="75%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <h3 id="microscaling-mx"><sub>Microscaling (MX)</sub></h3> <p>MX is standardized by the Open Compute Project, and widely supported by recent AI chips such as AMD Radeon, Meta MTIA, and Microsoft MAIA. In this approach, the high-precision scale factor is quantized to an 8-bit power-of-two (E8M0), as described in the following equations:</p> \[\mathrm{S} = \frac{ |\mathrm{B}|_{\text{max}} }{ \mathrm{Q}_{\text{max}} } ; \ \ \boldsymbol{\mathrm{S_{Q}}} = 2^{\left\lceil\,\texttt{log2}\left( \,\mathrm{S} \,\right) \,\right\rceil} ; \ \ \mathrm{B_\mathrm{S}} = \frac{\mathrm{B}}{\boldsymbol{\mathrm{S_{Q}}}} ; \ \ \mathrm{B_Q} = \texttt{Round}\left(\,\mathrm{B_\mathrm{S}}\,,\, \mathrm{Q}\,\right) ; \ \ \mathrm{B_\mathrm{D}} = \mathrm{B_Q} \cdot \boldsymbol{\mathrm{S_{Q}}}\] <p>where \(\mathrm{B}\) is the block to be quantized. After calculating the scale factor \(\mathrm{S}\), we take its binary logarithm followed by a ceiling operation to obtain the integer exponent<d-footnote>When implementing this step in code, there is no need to explicitly compute the logarithm, which is very costly on hardware. Instead, this whole step can be efficiently implemented at the binary level using simple bitwise operations, as described in <a href="https://huggingface.co/deepseek-ai/DeepSeek-V3.2/blob/main/inference/kernel.py#L20">this code snippet</a> from DeepSeek.</d-footnote>. The quantized scale factor is the base-2 exponentiation of this integer exponent. The ceiling operation ensures that \(\mathrm{S}\) is always <em>rounded up</em> to the nearest power-of-two that is larger than or equal to \(\mathrm{S}\). This is different from the rounding mechanism for the scaled block element \(\mathrm{B_{S}\,}\), which is <em>rounded (up or down)</em> to the nearest quantization value.</p> <p>The purpose of rounding up during power-of-two scale factor quantization, is to ensure the scaled block \(\mathrm{B_{S}}\) can always remains within the representable quantization range without overflow. Specifically, we want to ensure that:</p> \[\begin{aligned} |\mathrm{B_{S}}| \leq \mathrm{Q}_{\text{max}} \iff |\mathrm{B_{\text{max},\,S}}| \leq \mathrm{Q}_{\text{max}} \iff \frac{|\mathrm{B_{\text{max}}}|}{\mathrm{S_{Q}}} \leq \mathrm{Q}_{\text{max}} \iff \mathrm{S_{Q}} \geq \frac{|\mathrm{B_{\text{max}}}|}{\mathrm{Q}_{\text{max}}} \iff \mathrm{S_{Q}} \geq\, \mathrm{S} \end{aligned}\] <p>Thus, to avoid overflow, the quantized scale factor should be larger than or equal to the original scale factor.</p> <p>Interested readers may ask: <strong>Why don’t we use the normal rounding (up or down) mechanism when rounding the scale factor to its nearest power-of-two?</strong> In fact, this is exactly what <a href="https://github.com/microsoft/microxcaling/blob/7bc41952de394f5cc5e782baf132e7c7542eb4e4/mx/mx_ops.py#L173">the original MX quantizer from Microsoft</a> does. However, this naive approach can cause significant overflow of the scaled block, \(\mathrm{B_{S}\,}\), leading to large saturation error<d-footnote>Hence, you should never refer to that original implementation for MX quantization.</d-footnote>. To better explain this problem, I will use the popular MXFP4 format as an example, where each block is quantized to FP4-E2M1 that can represent \(\{\,0\,, \pm\,0.5\,, \pm\,1\,, \pm\,1.5\,, \pm\,2\,, \pm\,3\,, \pm\,4\,, \pm\,6\,\}\). Let’s analyze what happens to the block maximum, \(\mathrm{B}_{\text{max}\,}\), when the scale factor is rounded (up or down) to its nearest power-of-two. For simplicity, I will assume \(\mathrm{B}_{\text{max}}\) is a positive normal FP32 value, leading to the following equations for computing the scaled block maximum, \(\mathrm{B_{\text{max},\,S}}\):</p> \[\mathrm{S} = \frac{\mathrm{B}_{\text{max}}}{6} ; \ \ \mathrm{S_{Q}} = 2^{\left\lfloor\,\texttt{log2}\left( \,\mathrm{S} \,\right) \,\right\rceil} ; \ \ \mathrm{B_{\text{max},\,S}} = \frac{\mathrm{B}_{\text{max}}}{\mathrm{S_{Q}}}\] <p>Since \(\mathrm{B}_{\text{max}}\) is a positive normal FP32 value, I can write it in floating-point representation as described in <a href="#i-2-floating-point-fp">Section I-2</a>:</p> \[\mathrm{B_{\text{max}}} = 0\;\mathrm{\underbrace{E_{7}\dots E_{0}}_{\text{Exponent}} \; \underbrace{M_{22}\dots M_{0}}_{\text{Mantissa}}} = (-1)^0 \cdot 2^{\mathrm{E}-127} \cdot (\,1+\mathrm{M}\,) = 2^{\mathrm{E\,'}} \cdot \mathrm{M\,'}\] <p>where \(\mathrm{E\,'}\) is the actual exponent after subtracting the bias \(127\), and \(\mathrm{M\,'}\) is the actual mantissa after adding the hidden bit \(1\) for normal values. Using this representation, the scale factor can be expressed as:</p> \[\mathrm{S} = \frac{\mathrm{B}_{\text{max}}}{6} = \frac{ 2^{\,\mathrm{E\,'}} \cdot \mathrm{M\,'} }{ 2^2 \cdot 1.5 } = 2^{\,\mathrm{E\,'} - \,2} \cdot \frac{\mathrm{M\,'}}{1.5}\] <p>Now, assume \(\mathrm{M\,'} &gt; 1.5\), which accounts for 50% of possible cases<d-footnote>Assume the mantissa is uniformly distributed between [1, 2), then we have 50% probability that the mantissa is larger than 1.5</d-footnote>, and since \(\mathrm{M\,'} &lt; 2\) by definition of floating-point, we have:</p> \[\begin{aligned} &amp; 2^{\,\mathrm{E\,'} - \,2} \cdot \frac{1.5}{1.5} \,&lt;\, \mathrm{S} \,&lt;\, 2^{\,\mathrm{E\,'} - \,2} \cdot \frac{2}{1.5} \\[0.3em] \iff\; &amp; \mathrm{E\,'} - 2 \,&lt;\, \texttt{log2}\left( \,\mathrm{S} \,\right) \,&lt;\, \mathrm{E\,'} - 2 + 0.415 \\[0.3em] \implies\; &amp; \left\lfloor\,\texttt{log2}\left(\,\mathrm{S} \,\right) \,\right\rceil \,=\, \mathrm{E\,'} - 2 \\[0.3em] \implies\; &amp; \mathrm{S_{Q}} \,=\, 2^{\left\lfloor\,\texttt{log2}\left(\,\mathrm{S} \,\right) \,\right\rceil} \,=\, 2^{\mathrm{E\,'} - 2} \\ \implies\; &amp; \mathrm{B_{\text{max},\,S}} \,= \frac{\mathrm{B}_{\text{max}}}{\mathrm{S_{Q}}} =\, \frac{2^{\mathrm{E\,'}} \cdot \mathrm{M\,'}}{2^{\mathrm{E\,'} - 2}} = \, 4\,\mathrm{M\,'} \in\, (\,6,\,8\,) \\[0.3em] \implies\; &amp; \mathrm{B_{\text{max},\,Q}} = \texttt{Round}\left(\,\mathrm{B_{\text{max},\,S}}\,,\, \text{FP4}\,\right) = 6 \end{aligned}\] <p>The above derivation implies that: If the mantissa of \(\mathrm{B}_{\text{max}}\) is larger than 1.5, then \(\mathrm{B_{\text{max},\,S}}\) will overflow outside the FP4 range<d-footnote>The above analysis can be generalized to other number representations under MX quantization: If the mantissa of B_max is larger than the mantissa of Q_max (e.g., 1.5 for FP4), then rounding (up or down) the scale factor to its nearest power-of-two will cause B<sub>max, S</sub> to overflow outside the representable quantization range.</d-footnote>. Consequently, the quantized block maximum, \(\mathrm{B_{\text{max},\,Q\,}}\), is always mapped / clamped to the largest representable FP4 value, \(6\), leading to saturation error. On the other hand, if we only round up the scale factor to its nearest power-of-two, we have:</p> \[\begin{aligned} &amp; 2^{\,\mathrm{E\,'} - \,2} \cdot \frac{1.5}{1.5} \,&lt;\, \mathrm{S} \,&lt;\, 2^{\,\mathrm{E\,'} - \,2} \cdot \frac{2}{1.5} \\[0.3em] \iff\; &amp; \mathrm{E\,'} - 2 \,&lt;\, \texttt{log2}\left( \,\mathrm{S} \,\right) \,&lt;\, \mathrm{E\,'} - 2 + 0.415 \\[0.3em] \implies\; &amp; \left\lceil\,\texttt{log2}\left( \,\mathrm{S} \,\right) \,\right\rceil \,=\, \mathrm{E\,'} - 1 \\[0.3em] \implies\; &amp; \mathrm{S_{Q}} \,=\, 2^{\left\lceil\,\texttt{log2}\left( \,\mathrm{S} \,\right) \,\right\rceil} \,=\, 2^{\mathrm{E\,'} - 1} \\ \implies\; &amp; \mathrm{B_{\text{max},\,S}} \,= \frac{\mathrm{B}_{\text{max}}}{\mathrm{S_{Q}}} =\, \frac{2^{\mathrm{E\,'}} \cdot \mathrm{M\,'}}{2^{\mathrm{E\,'} - 1}} = \, 2\,\mathrm{M\,'} \in\, (\,3,\,4\,) \\[0.3em] \implies\; &amp; \mathrm{B_{\text{max},\,Q}} = \texttt{Round}\left(\,\mathrm{B_{\text{max},\,S}}\,,\, \text{FP4}\,\right) = 3 \,\ \text{or} \,\ 4 \end{aligned}\] <p>Now, \(\mathrm{B_{\text{max},\,Q}}\) can be mapped to two representable FP4 values: \(3\) or \(4\), whichever is closer to \(\mathrm{B_{\text{max},\,S}}\). This offers more flexibility compared to the naive rounding mechanism, where \(\mathrm{B_{\text{max},\,Q}}\) can only be mapped / clamped to \(6\).</p> <p>The above analysis also indicates a hardware-efficient strategy for calculating the block scale of MXFP4:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">if </span><span class="p">(</span><span class="n">B_max</span><span class="p">.</span><span class="n">man</span> <span class="o">&gt;</span> <span class="mf">1.5</span><span class="p">):</span>
  <span class="n">block_exp</span> <span class="o">=</span> <span class="n">B_max</span><span class="p">.</span><span class="n">exp</span> <span class="o">-</span> <span class="mi">1</span> 
<span class="k">else</span><span class="p">:</span> <span class="c1"># (B_max.man &lt;= 1.5)
</span>  <span class="n">block_exp</span> <span class="o">=</span> <span class="n">B_max</span><span class="p">.</span><span class="n">exp</span> <span class="o">-</span> <span class="mi">2</span>

<span class="n">block_scale</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">**</span> <span class="n">block_exp</span>
</code></pre></div></div> <p>which completely eliminates the division (\(\mathrm{B_{\text{max}}}\,/\,6\)), logarithm, and ceiling operations to compute the block scale. Assume the block element is represented in BF16-E8M7, then a SystemVerilog implementation can be:</p> <div class="language-verilog highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">input</span>  <span class="kt">logic</span> <span class="p">[</span><span class="mi">15</span><span class="o">:</span><span class="mi">0</span><span class="p">]</span> <span class="n">B_max</span><span class="p">;</span>
<span class="kt">output</span> <span class="kt">logic</span> <span class="p">[</span><span class="mi">8</span><span class="o">:</span><span class="mi">0</span><span class="p">]</span>  <span class="n">block_exp</span><span class="p">;</span>
<span class="kt">output</span> <span class="kt">logic</span> <span class="p">[</span><span class="mi">15</span><span class="o">:</span><span class="mi">0</span><span class="p">]</span> <span class="n">block_scale</span><span class="p">;</span>

<span class="k">always_comb</span> <span class="k">begin</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">B_max</span><span class="p">[</span><span class="mi">6</span><span class="o">:</span><span class="mi">5</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mb">2'b10</span><span class="p">)</span> <span class="c1">// Think why this is equivalent to (B_max.man &gt; 1.5)</span>
      <span class="n">block_exp</span> <span class="o">=</span> <span class="n">B_max</span><span class="p">[</span><span class="mi">14</span><span class="o">:</span><span class="mi">7</span><span class="p">]</span> <span class="o">-</span> <span class="mi">1</span> 
  <span class="k">else</span>
      <span class="n">block_exp</span> <span class="o">=</span> <span class="n">B_max</span><span class="p">[</span><span class="mi">14</span><span class="o">:</span><span class="mi">7</span><span class="p">]</span> <span class="o">-</span> <span class="mi">2</span>
<span class="k">end</span>

<span class="k">assign</span> <span class="n">block_scale</span> <span class="o">=</span> <span class="o">{</span><span class="mb">1'b0</span><span class="p">,</span> <span class="n">block_exp</span><span class="p">,</span> <span class="mi">7'd0</span><span class="o">}</span><span class="p">;</span>
</code></pre></div></div> <p>To measure the algorithmic performance of MX under the two rounding approaches for scale factor quantization, I implement <a href="https://github.com/abdelfattah-lab/NVFP4-RaZeR/blob/5c6857a8fbbcd85e9adc47701e85992fdb1a3217/quantize/quantizer.py#L93">the naive MXFP4 quantizer</a> and <a href="https://github.com/abdelfattah-lab/NVFP4-RaZeR/blob/5c6857a8fbbcd85e9adc47701e85992fdb1a3217/quantize/quantizer.py#L139">the enhanced MXFP4 quantizer</a>. The following table shows the perplexity<d-footnote>Perplexity is a widely used metric to quantify the LLM performance, and <b>lower perplexity means better performance</b>.</d-footnote> of Wikitext-2 dataset on several Llama3 and Qwen3 models, under MXFP4 weight-activation quantization with a block size of 16. The round-up approach achieves much better perplexity compared to the naive round-to-nearest approach for power-of-two scale factor quantization.</p> <table style="width: 80%; border-spacing: 5px; margin-left: 1em; margin-top: -1em; margin-bottom: 1.5em;"><thead> <tr> <th> </th> <th> <br/>Llama-3.1-8B </th> <th> <br/>Llama-3.2-3B </th> <th> <br/>Qwen3-4B </th> <th> <br/>Qwen3-8B </th> </tr></thead> <tbody> <tr> <td> Naive Round </td><td> 9.51 </td><td> 15.35 </td><td> 17.46 </td><td> 11.84 </td> </tr> <tr> <td> Round-Up </td><td> 8.69 </td><td> 13.58 </td><td> 15.69 </td><td> 10.66 </td> </tr> </tbody></table> <h3 id="b-nvidia-nv-approach"><sub>b. NVIDIA (NV) Approach</sub></h3> <p>Recall from <a href="#iii-1-block-wise-quantization">Section III-1</a>, if the scale factor is accurately represented (e.g., in FP32), then the maximum element can be accurately quantized without error. However, the E8M0 scale factor used in MX violates this condition and introduces error for the block’s maximum element<d-footnote>Again, the "maximum element" refers to the element with maximum magnitude.</d-footnote>. This raises the question: <strong>Can we minimize the quantization error of block maximum while still using an 8-bit scale factor?</strong></p> <p>Let’s try to understand the limitations of MX when quantizing the block maximum, \(\mathrm{B}_{\text{max}}\). Again, for simplicity, assume \(\mathrm{B}_{\text{max}}\) is a positive normal FP32 value:</p> \[\mathrm{B_{\text{max}}} = 0\;\mathrm{\underbrace{E_{7}\dots E_{0}}_{\text{Exponent}} \; \underbrace{M_{22}\dots M_{0}}_{\text{Mantissa}}} = (-1)^0 \cdot 2^{\mathrm{E}-127} \cdot (\,1+\mathrm{M}\,) = 2^{\mathrm{E\,'}} \cdot \mathrm{M\,'}\] <p>Since the scale factor of MX is power-of-two, scaling \(\mathrm{B}_{\text{max}}\) will only change its exponent without affecting mantissa:</p> \[\mathrm{B_{\text{max},\,S}} \,= \frac{\mathrm{B}_{\text{max}}}{\mathrm{S_{MX}}} \,= \frac{2^{\mathrm{E\,'}} \cdot \mathrm{M\,'}}{\mathrm{S_{MX}}} = \, 2^{\widetilde{\mathrm{E}}} \cdot \mathrm{M\,'}\] <p>Comparing the above result with the case of accurate FP32 scaling:</p> \[\mathrm{S}_{\text{FP32}} = \frac{ \mathrm{B}_{\text{max}} }{ \mathrm{Q}_{\text{max}} }; \ \ \ \mathrm{B_{\text{max},\,S}} = \frac{ \mathrm{B}_{\text{max}}}{\mathrm{S}_{\text{FP32}}} = \mathrm{Q}_{\text{max}}\] <p>We observe the accurate scaling gives \(\mathrm{B_{\text{max},\,S}} = \mathrm{Q}_{\text{max}\,}\), which implies the mantissa of \(\mathrm{B_{\text{max},\,S}}\) is equal to that of \(\mathrm{Q}_{\text{max}}\) (e.g., \(1.5\) for FP4). However, under MX scaling, the mantissa of \(\mathrm{B_{\text{max},\,S}}\) is equal to that of \(\mathrm{B_{\text{max}}\,}\), which can take any value in \([\,1.0,\, 2.0\,)\). Consequently, MX can be viewed as effectively quantizing the mantissa of \(\mathrm{B_{\text{max}}}\) to the representable mantissa of \(\mathrm{Q}\)<d-footnote>For example, with the MXFP4 quantization described just now, B<sub>max</sub> can be quantized to 3, 4, or 6. These three quantization values have two mantissa, 1.0 and 1.5, which are the representable mantissas of FP4.</d-footnote>, resulting in quantization error on \(\mathrm{B}_{\text{max}}\). To reduce this error, one approach is to make the mantissa of \(\mathrm{B_{\text{max}}}\) closer to that of \(\mathrm{Q}_{\text{max}}\) through additional mantissa scaling, which requires the scale factor to also contain mantissa bits<d-footnote>For example, assume B<sub>max</sub> = 2<sup>E'</sup> × M' has a mantissa M' = 1.875. Under MXFP4 quantization, S = 2<sup>E' - 1</sup> and B<sub>max, S</sub> = 3.75 will be rounded to 4, leading to an error of 0.25. But if S can store a 2-bit mantissa, then S = 2<sup>E' - 2</sup> × 1.25 and B<sub>max, S</sub> = 6 can be accurately quantized without error</d-footnote>.</p> <p>Building on this insight, NVIDIA (NV) proposes to employ FP8-E4M3, the 8-bit floating-point format with 4-bit exponent and 3-bit mantissa, for block scale quantization, as described in the following equations:</p> \[\begin{aligned} &amp;\mathrm{S} = \frac{ |\mathrm{B}_{\text{max}}| }{ \mathrm{Q}_{\text{B,max}} } \\[0.3em] &amp;\mathrm{S'} = \frac{ \mathrm{S}_{\text{max}} }{ \mathrm{Q}_{\text{S,max}} } = \left(\frac{ |\mathrm{B}_{\text{max}}| }{ \mathrm{Q}_{\text{B,max}}}\right)_{\text{max}} \cdot \frac{1}{\mathrm{Q}_{\text{S,max}} } = \frac{|\mathrm{B}_{\text{max}}|_{\text{max}}}{\mathrm{Q}_{\text{B,max}}} \cdot \frac{1}{\mathrm{Q}_{\text{S,max}}} = \frac{|\mathrm{T}_{\text{max}}|}{\mathrm{Q}_{\text{B,max}} \cdot\mathrm{Q}_{\text{S,max}}} \\[0.3em] &amp;\mathrm{S_\mathrm{S}} = \frac{\mathrm{S}}{\mathrm{S'}} \\[0.3em] &amp;\mathrm{S_Q} = \texttt{Round}\left(\,\mathrm{S_\mathrm{S}}\,,\, \text{FP8}\,\right) \\[0.3em] &amp;\mathrm{S_\mathrm{D}} = \mathrm{S_Q} \cdot \mathrm{S'} \end{aligned}\] <p>where \(\mathrm{Q}_{\text{B,max}}\) is the maximum quantization value for the block element, \(\mathrm{Q}_{\text{S,max}}\) is the maximum quantization value for the block scale (i.e., \(448\) for FP8), \(\mathrm{S}'\) is the scale of block scale, \(\mathrm{T}_{\text{max}}\) is the tensor maximum, \(\mathrm{S_{S}}\) is the scaled block scale, \(\mathrm{S_{Q}}\) is the quantized block scale, and \(\mathrm{S_{D}}\) is the final dquantized block scale. This block scale quantization process is exactly the same as how we quantize a tensor as discussed in <a href="#ii-2-mathematical-background">Section II-2</a>. The mathematical equations are therefore also the same as those for tensor quantization, except that we change \(\mathrm{X}\) to \(\mathrm{S}\), \(\mathrm{S}\) to \(\mathrm{S'}\), and \(\mathrm{Q}\) to FP8. Below is a visualization to help you understand the FP8 block scale quantization using the popular NVFP4 format (i.e., \(\mathrm{Q}_{\text{B,max}}\) = 6) as an example.</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/NVFP4_Scale_Quant.png" width="100%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>The above equation for calculating \(\mathrm{S'}\) indicates a <strong>tensor-wise scaling</strong>, as also described in <a href="https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/">NVIDIA’s blog</a><d-footnote>Personally, I find NVIDIA’s description of tensor-wise scaling somewhat vague; hopefully, my explanation provides greater clarity.</d-footnote>. The reason is that, when following the standard quantization procedure to quantize block scales, we meed to determine the maximum across all block scales. Since each block scale equals to its block maximum divided by \(6\), this reduces to finding the maximum of all block maxima divided by \(6\), which is simply the tensor maximum divided by \(6\).</p> <p>You may also wonder: <strong>Why choosing E4M3 instead of other FP8 variants such as E5M2 / E3M4 when quantizing the block scale?</strong> The reason is that, empirically, E4M3 can better fit the numerical distribution of block maxima<d-footnote>According to our discussion in [Section II-2](#ii-2-mathematical-background), quantizing block scales is equivalent to quantizing block maxima, since the latter differs from the former only by a constant factor (i.e., $$Q_<sub>B,max </sub>$$).</d-footnote> in LLM weight and activation tensors, resulting in lower quantization error than other FP8 variants. This brings another important topic, <em>Quantization Format for Tensor Elements</em>, which I will cover soon.</p> <p>Thanks to the capability of mantissa scaling, NV reduces the quantization error of scale factors compared to MX, which in turn reduces the quantization error of block elements. To quantify these benefits, I implement <a href="https://github.com/abdelfattah-lab/NVFP4-RaZeR/blob/5c6857a8fbbcd85e9adc47701e85992fdb1a3217/quantize/quantizer.py#L375">the NVFP4 quantizer</a> and compare it with <a href="https://github.com/abdelfattah-lab/NVFP4-RaZeR/blob/5c6857a8fbbcd85e9adc47701e85992fdb1a3217/quantize/quantizer.py#L139">the enhanced MXFP4 quantizer</a> discussed in the last section. The following table shows the perplexity of Wikitext-2 dataset on several Llama3 and Qwen3 models, under NVFP4 and MXFP4 weight-activation quantization with a block size of 16. NVFP4 achieves much better perplexity than MXFP4.</p> <table style="width: 80%; border-spacing: 5px; margin-left: 1em; margin-top: -1em; margin-bottom: 1.5em;"><thead> <tr> <th> </th> <th> <br/>Llama-3.1-8B </th> <th> <br/>Llama-3.2-3B </th> <th> <br/>Qwen3-4B </th> <th> <br/>Qwen3-8B </th> </tr></thead> <tbody> <tr> <td> Enhanced MXFP4 </td><td> 8.69 </td><td> 13.58 </td><td> 15.69 </td><td> 10.66 </td> </tr> <tr> <td> NVFP4 </td><td> 7.90 </td><td> 11.97 </td><td> 13.88 </td><td> 10.05 </td> </tr> </tbody></table> <h3 style="margin-top: 1rem; margin-bottom: -0.25rem;">Hardware Implication of MX and NV</h3> <p>The different approaches of MX and NV for block scale quantization introduce a trade-off between model accuracy and computation cost<d-footnote>Assuming both approaches use the same block size, their memory cost is roughly the same. NV requires an additional tensor-wise FP32 scale, but that single scale has negligible memory cost.</d-footnote>. Although NV achieves better model accuracy than MX, it incurs higher hardware cost to perform dequantization, as visualized below:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/MXFP4_NVFP4_Hardware.png" width="90%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>Consider the popular FP4 block-wise quantization under a block size of 16. Let’s analyze the hardware cost of performing block-wise GEMM with the two scale factor quantization approaches:</p> <ul> <li><strong>Block Dot-product</strong>: Since the maximum and minimum FP4 values are \(\pm6\) and \(\pm0.5\), a single FP4 multiplication ranges from \(\pm0.25\) to \(\pm36\), which can be represented using 9-bit fixed-point. With a block size of 16, the dot-product result becomes 13 bits.</li> <li> <p><strong>MX Dequantization</strong>: To analyze the hardware cost, let’s first write out the binary / decimal values of block scale and dot-product output:</p> \[\begin{aligned} \mathrm{S_{W}} &amp;=\, \mathrm{E_{W}} =\, \mathrm{\underbrace{E_{7}\dots E_{0}}_{\text{Exponent}}} \,=\, 2^{\mathrm{E_{W}}-127} \\[0.3em] \mathrm{S_{A}} &amp;=\, \mathrm{E_{A}} =\, \mathrm{\underbrace{E_{7}\dots E_{0}}_{\text{Exponent}}} \,=\, 2^{\mathrm{E_{A}}-127} \\[0.3em] \mathrm{O} \,&amp;=\, \mathrm{S \underbrace{M_{11}\dots M_{0}}_{\text{Un-norm Mantissa}}} =\, (-1)^{\,\mathrm{S}} \cdot \left(\,\Sigma_{j=0}^{11}\,2^{j-2}\cdot\mathrm{M}_{j} \right) \end{aligned}\] <p>Based on the above equations, the dequantized output should be: \(\mathrm{O_{D}} \,=\, (-1)^{\,\mathrm{S}} \cdot\, 2^{\mathrm{E_{W}} + \mathrm{E_{A}}-254} \cdot \left(\,\underbrace{\Sigma_{j=0}^{11}\,2^{j-2} \cdot \mathrm{M}_{j}}_{\text{Un-norm Mantissa}} \,\right)\)</p> <p>Interestingly, this expression has a very similar structure to the standard 32-bit floating-point representation discussed in <a href="#i-2-floating-point-fp">Section I-2</a>:</p> \[\text{FP32} \,=\, \mathrm{\underbrace{S}_{Sign} \; \underbrace{E_7\dots E_{0}}_{\text{Exponent}} \; \underbrace{M_{22}\dots M_{0}}_{\text{Mantissa}}} \,= \,(-1)^{\mathrm{S}} \cdot\, \mathrm{2^{E-127} \cdot 1.{M}}\] <p>The dequantized output sign is the FP32 sign, the FP32 exponent \(\mathrm{E} = \mathrm{E_{W}} + \mathrm{E_{A}} - 127\) is calculated via unsigned integer addition, and the FP32 mantissa is obtained via normalizing the dequantized output mantissa. This normalization only requires low-cost hardware components such as Leading-One Detector and Shifter. Finally, the three binary components: sign, exponent, mantissa of dequantized output, are concatenated together (via some simple wiring in hardware) to produce a FP20-E8M11<d-footnote>The dequantized output mantissa has 12 bits, but recall the floating-point representation has a hidden mantissa bit that is not stored.</d-footnote> number for cross-block partial sum accumulation. To summarize, MX dequantization has low computation cost, involving mainly unsigned integer addition, a leading-one detector, and a shifter.</p> </li> <li> <p><strong>NV Dequantization</strong>: Again, let’s write out the binary / decimal values of block scale and dot-product output:</p> \[\begin{aligned} \mathrm{S_{W}} &amp;=\, \mathrm{\underbrace{E_{3}\dots E_{0}}_{\text{Exponent}} \;\underbrace{M_{2}\dots M_{0}}_{\text{Mantissa}}} \,=\, 2^{\mathrm{E_{W}}-7} \cdot 1.\mathrm{M_{W}} \\[0.3em] \mathrm{S_{A}} &amp;=\, \mathrm{\underbrace{E_{3}\dots E_{0}}_{\text{Exponent}} \;\underbrace{M_{2}\dots M_{0}}_{\text{Mantissa}}} \,=\, 2^{\mathrm{E_{A}}-7} \cdot 1.\mathrm{M_{A}} \\[0.3em] \mathrm{O} \,&amp;=\, \mathrm{S \underbrace{M_{11}\dots M_{0}}_{\text{Un-norm Mantissa}}} =\, (-1)^{\mathrm{S}} \cdot \left(\,\Sigma_{j=0}^{11}\,2^{j-2}\cdot\mathrm{M}_{j} \right) \end{aligned}\] <p>Note that the scale factor’s binary encoding does not need the sign bit, as it is always positive by definition of quantization. Based on the above equations, the dequantized output should be:</p> \[\mathrm{O_{D}} \,=\, (-1)^{\mathrm{S}} \cdot\, 2^{\mathrm{E_{W}} + \mathrm{E_{A}}-14} \cdot \left[\,1.\mathrm{M_{W}} \cdot 1.\mathrm{M_{A}} \cdot \left(\,\Sigma_{j=0}^{11}\,2^{j-2}\cdot\mathrm{M}_{j}\,\right) \,\right]\] <p>Similar to MX, this expression closely matches the standard floating-point representation. The dequantized output sign is the FP sign, and the FP exponent \(\mathrm{E} = \mathrm{E_{W}} + \mathrm{E_{A}} - 14\) is calculated via unsigned integer addition. However, calculating the dequantized output mantissa is more complicated, which involves a 4-bit multiplication between the two scale factors’ mantissa, followed by multiplying the 12-bit output mantissa. Then, the dequantized output mantissa is normalized to the standard FP mantissa via leading-one detector and shifter. Finally, the three binary components: sign, exponent, mantissa of dequantized output, are concatenated together to produce a FP24-E5M19 number for cross-block partial sum accumulation.</p> </li> </ul> <p>Based on the above analysis, the NV dequantizer differs from the MX dequantizer in two notable aspects: (1) It uses a slightly cheaper adder (4-bit vs. 8-bit) for exponent addition; (2) But it requires much more expensive multiplier (\(4\text{-bit} \times 4\text{-bit} \times 12\text{-bit}\)) for mantissa multiplication. Thus, the GEMM hardware<d-footnote>Also called tensor core in many AI chips.</d-footnote> of NV consumes larger area than that of MX<d-footnote>For example, according to <a href="https://arxiv.org/abs/2603.08713">this paper from Meta</a>, NVFP4 incurs 12.6% total GEMM hardware area overhead relative to MXFP4, under the same block size of 16.</d-footnote>.</p> <h2 id="iii-3-hierarchical-scaling-for-mxfp4"><sub>III-3. Hierarchical Scaling for MXFP4</sub></h2> <p>As discussed in the previous section, NV quantization requires an FP32 tensor scale in addition to the FP8-E4M3 block scale. This multi-level scheme is commonly referred to as <strong>hierarchical scaling</strong>. The main reason behind NV’s hierarchical scaling is that, the FP8-E4M3 block scale alone may not fully cover a tensor’s dynamic range<d-footnote>For example, under NVFP4 quantization, the FP8-E4M3 scale has a dynamic range of [<sup> </sup>2<sup>-9</sup>, 448<sup> </sup>]. Multiplying this scale with the FP4 element yields a dynamic range of [<sup> </sup>2<sup>-10</sup>, 2688<sup> </sup>], which is insufficient to represent LLM tensors.</d-footnote>. On the other hand, MX quantization only requires a single-level block-wise scaling, due to the large dynamic range enabled by its E8M0 block scale<d-footnote>For example, under MXFP4 quantization, the E8M0 scale has a dynamic range of [<sup> </sup>2<sup>-126</sup>, 2<sup>125</sup><sup> </sup>]. Multiplying this scale with the FP4 element yields a dynamic range of [<sup> </sup>2<sup>-127</sup>, 1.5×2<sup>127</sup><sup> </sup>], which is very close to BF16's dynamic range [<sup> </sup>2<sup>-133</sup>, 1.9921875×2<sup>127</sup><sup> </sup>].</d-footnote>. This difference naturally raises the question: <strong>Can MX also employ hierarchical scaling?</strong> This section presents two case studies from Meta and DeepSeek, demonstrating how hierarchical scaling can enhance MXFP4 to achieve better model accuracy and hardware efficiency.</p> <h3 style="margin-top: 1rem; margin-bottom: -0.25rem;">Meta's MXFP4 Quantization Recipe</h3> <p>At first glance, hierarchical scaling may seem unnecessary for MXFP4, since the E8M0 block scale already covers the tensor’s full dynamic range. However, as discussed in <a href="#iii-2-scale-factor-quantization">Section III-2</a>, one way to reduce the MX quantization error is to bring the mantissa of block maximum closer to that of quantization maximum through additional mantissa scaling, which requires the scale factor to contain mantissa bits. However, directly adding mantissa bits to the E8M0 block scale introduces significant memory and computation overhead during dequantization.</p> <p>To enable mantissa scaling for MX without sacrificing hardware efficiency, Meta proposes a two-level <a href="https://arxiv.org/abs/2603.08713">Macro Block Scaling</a> (MBS) scheme. First, a macro-block (MB) of size \(1 \times 128\) is scaled using an 8-bit mantissa (E0M8), aligning the mantissa of its maximum value close to \(1.5\) (i.e., the mantissa of FP4 maximum). The macro-block is then partitioned into eight local-blocks (LB) of size \(1 \times 16\), each applied MX scaling with an E8M0 block scale. This procedure is visualized below:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Meta_MXFP4_Software.png" width="75%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>where the E0M8 macro-block scale is obtained by extracting the top 8 mantissa bits of the FP32 scale, i.e., \(\frac{\texttt{max}(\,\mathrm{|MB|}\,)}{1.5} \text{ &amp; 0x007f8000}\). </p> <p><strong>Why Does MBS Help?</strong> Let’s analyze how MBS affects the macro-block maximum, \(\mathrm{B_M}_{\text{max}}\). Assume \(\mathrm{B_M}_{\text{max}}\) is a positive normal FP32 value, leading to the following equations for MBS:</p> \[\begin{aligned} &amp;\mathrm{B_M}_{\text{max}} = 2^{\mathrm{E'}} \cdot \mathrm{M'} \quad \text{(Assume positive normal value.)} \\[0.1em] &amp;\mathrm{S_{B_M}} = \left(\frac{ \mathrm{B_M}_{\text{max}} }{ 1.5 }\right) = \begin{cases} \; 2^{\mathrm{E'}} \times \left( \frac{\mathrm{M'}}{1.5} \right) &amp; \text{if } \mathrm{M'} \geq 1.5 \\[0.1em] \; 2^{\mathrm{E'}-1} \times \left( \frac{\mathrm{M'}}{1.5} \cdot 2 \right) &amp; \text{if } \mathrm{M'} &lt; 1.5 \end{cases} \\[0.3em] &amp;\text{(Assume retaining the top 8 bits can sufficiently preserve the whole mantissa.)} \\ &amp;\mathrm{S_{B_M}} = \begin{cases} \; \frac{\mathrm{M'}}{1.5} &amp; \text{if } \mathrm{M'} \geq 1.5 \\[0.1em] \; \frac{\mathrm{M'}}{1.5} \cdot 2 &amp; \text{if } \mathrm{M'} &lt; 1.5 \end{cases} \\[0.3em] &amp;\mathrm{B_{M,\,S}} = \frac{\mathrm{B_M}}{\mathrm{S_{B_M}}} = \begin{cases} \; 2^{\mathrm{E'}} \cdot 1.5 &amp; \text{if } \mathrm{M'} \geq 1.5 \\[0.1em] \; 2^{\mathrm{E' - 1}} \cdot 1.5 &amp; \text{if } \mathrm{M'} &lt; 1.5 \end{cases} \\[0.3em] \end{aligned}\] <p>The above analysis assumes that retaining the top 8 mantissa bits is sufficient to preserve the whole mantissa, which is generally true. Empirically, Meta finds that the top 8 mantissa bits can approximate the original FP32 mantissa with \(&lt;0.3\%\) error. After performing MBS, the scaled maximum \(\mathrm{B_{M,\,S}}\) has a mantissa of \(1.5\), which can be accrately quantized to 6.0 during the MXFP4 quantization of local-blocks.</p> <p>The benefits of MBS is two fold. First, it enables more accurate quantization of the macro-block maximum, thereby preserving the numerical afidelity of many high-magnitude outliers. Second, the \(1 \times 128\) macro-block size significantly reduces the memory and dequantization cost compared to allocating extra mantissa bits to the local-block scale.</p> <p><strong>Hardware Implication of MBS</strong>: While Meta claims that MBS does not require any change to the GEMM hardware (tensor core), it still necessitates a careful architecture co-design to balance the throughput between tensor core and vector core. Below is a visualization of the GEMM computation flow using MBS:</p> <div style="text-align:center;"> <img src="/assets/img/blog/quant_codesign/Meta_MXFP4_Hardware.png" width="100%"/> <figcaption style="font-size: 0.95em; margin-top: 8px;"></figcaption> </div> <p>Within a macro-block, the eight local-blocks are all quantized to MXFP4. Therefore, the macro-block dot product, comprising 128 MAC operations, can be accelerated using the dedicated MXFP4 tensor core. However, the macro-block output must be dequantized by multiplying it with two E0M8 scales, followed by accumulation across different macro-blocks. This process is realized on the FP32 vector core and involves 2 MAC operations<d-footnote>Technically, the macro-block dequantization involves 1 multiplication between the two E0M8 scales and 1 MAC with the macro-block output. However, most AI hardware performs fused MAC operations in a single cycle; if only a multiplication or addition is issued, the other part of the MAC unit becomes idle.</d-footnote>. In order to fully utilize the MXFP4 tensor core for GEMM, the vector core throughput should exceed \(1/64\) of the tensor core throughput<d-footnote>This requirement may not be met on some existing GPUs. For example, on <a href="https://www.nvidia.com/en-us/data-center/gb300-nvl72/?ncid=no-ncid">NVIDIA GB300 NVL72</a>, the FP32 vector core throughput is only 1/180 of the FP4 tensor core throughput.</d-footnote>.</p> <h2 id="acknowledgment">Acknowledgment</h2> <p>Special thanks to <a href="https://www.linkedin.com/in/xilai-dai">Xilai Dai</a> (Cornell University), <a href="https://micas.esat.kuleuven.be/team/chao-fang">Dr. Chao Fang</a> (KU Leuven), and <a href="https://www.mohsaied.com">Prof. Mohamed Abdelfattah</a> (Cornell University), for helping shape out the content of this blog.</p> ]]></content><author><name>Yuzong Chen</name></author><category term="Quantization"/><category term="CoDesign"/><summary type="html"><![CDATA[( NOTE: For mobile users, please view this blog in landscape mode for better readibility. )]]></summary></entry></feed>